---
const names = ['Voyas AI', 'Noozz AI', 'gitweb', 'CoPaw', 'autoresearch-mlx', 'swarm'];
---

<div class="marquee" aria-hidden="true">
  <div class="marquee-track">
    <div class="marquee-group">
      {names.map((name) => <span>{name}</span>)}
    </div>
    <div class="marquee-group">
      {names.map((name) => <span>{name}</span>)}
    </div>
  </div>
</div>
